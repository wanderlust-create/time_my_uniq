<!DOCTYPE html>
<html>

<head>
  <title>MyUniqTime</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    
    <meta name="viewport" content="width=device-width,initial-scale=1">
    
    <%= stylesheet_link_tag 'application' , media: 'all' %>
      <%= javascript_include_tag 'application' %>
</head>

<body>
  <div class="flash message container-fluid p-4 bg-dark text-white text-center">
    <% flash.each do |type, msg| %>
      <%= msg %>
        <% end %>
  </div>
  <span>
    <nav class="navbar bg-dark">
      <% if logged_in? %>
        <ul class="welcome p-3" style="color:#FFFFFF">
          <h1>Welcome, <%= current_user.user_name %>
          </h1>
        </ul>
        <ul class="navbar-nav ml-auto p-2">
          <div class="btn-group">
            <%= button_to "Take me to my dashboard", my_uniq_time_dashboard_index_path(current_user)  , method: :get, class: "btn btn-light" %>
            <%= button_to "Sign Out" , '/logout' , class: "btn btn-light" %>
          </div>
        </ul>
        <% else %>
          <h1>Join SPINSPOT!!</h1>
          <ul class="nav-item float-right p-5">
            <%= button_to "Log In/Sign Up With Google" , "/auth/google_oauth2" , method: :get,
              class: "btn btn-light" %>
          </ul>
          <% end %>
    </nav>
  </span>
  <%= yield %>
</body>

</html>

